{
  "private": true,
  "workspaces": [
    "addon",
    "docs",
    "test-app"
  ],
  "scripts": {
    "build": "pnpm run --filter addon build",
    "lint": "npm-run-all --aggregate-output --continue-on-error --parallel lint:*",
    "lint:addon": "yarn workspace ember-x-tabs run lint",
    "lint:test-app": "yarn workspace test-app run lint",
    "lint:fix": "npm-run-all --aggregate-output --continue-on-error --parallel lint:fix:*",
    "lint:fix:addon": "yarn workspace ember-x-tabs run lint:fix",
    "lint:fix:test-app": "yarn workspace test-app run lint:fix",
    "test": "npm-run-all --aggregate-output --continue-on-error --parallel test:*",
    "test:test-app": "yarn workspace test-app run test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}